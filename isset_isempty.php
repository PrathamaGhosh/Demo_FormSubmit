<?php
//isset()-used to return true if it's declaired and not null
//isempty()-used to return true if it's not declaired or false or null
// $name="phpcourse";
// $name="null";
// echo isset($name);
// foreach($_POST as $keys => $values){ //used to print the array format of a form data
//     echo "{$key}={$values}";

// }

// sanitizing or validate the user input data(under in isset method)
// $username=filter_input(INPUT_POST,"username",FILTER_SANITIZE_SPECIAL_CHARS/FILTER_SANITIZE_NUMBER_INT/FILTER_SANITIZE_EMAIL);

// echo "hi {$username}";



//include() in php-copies all the contains of the files and includes it in your php file
// sections of our website become reusable and changes only need to be made in one place
// include ("file_name");
?>